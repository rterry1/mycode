#1/bin/rm
#This script calls on the excutable called **rm** (remove), which is
#installed to the */bin* directory. When this script is run,
#bash will execute this script with *RM*, which will remove (delete)
#the script :p
